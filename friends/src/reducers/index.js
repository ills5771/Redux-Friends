const initialState = {
  // getFriends: false,
  // isLoggedIn: false,
  friends: []
  //   addingFriend,
  // error: null
};

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
